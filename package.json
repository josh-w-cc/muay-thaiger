{
  "name": "starter-kit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "npm run lint --prefix api && npm run lint --prefix web && npm run lint --prefix playwright",
    "prepare": "npm ci --prefix api && npm ci --prefix web && npm ci --prefix playwright",
    "test": "npm test --prefix api && npm test --prefix shared && npm test --prefix web"
  }
}
